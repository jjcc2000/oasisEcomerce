{
  "compilerOptions": {
    "baseUrl": ".",
    "jsx": "react-jsx", // or "react" if using older React versions

    "paths": {
      "*": ["types/*", "*"]
    }
  },
  "include": ["src", "types"]
}
